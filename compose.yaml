services:
  catalogdb:
    image: postgres
  
  basketdb:
    image: postgres
  
  catalog.api:
    image: ${DOCKER_REGISTRY-}catalog.api
    build:
      context: .
      dockerfile: src/Services/Catalog/Catalog.Api/Dockerfile

volumes:
  postgres_catalog:
  postgres_basket:
